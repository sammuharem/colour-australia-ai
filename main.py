from australia import Australia, AustraliaEnvState
from solver.bfs import bfs
from solver.dfs import dfs

environment = Australia()
# solution = bfs(environment)
# solution2 = dfs(environment)

# parent = solution2.parent
# node = solution2
# while parent is not None:
#     print(node.state)
#     node = node.parent